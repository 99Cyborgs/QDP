# Experimental

Experimental package-owned materials assets belong here when they are useful to the canonical package but are not yet promoted to stable authority.

Current note:

- QDP MM lineage is being preserved as experimental context and migration notes, not as a second implementation authority
- branch-specific packs that remain non-reusable should move to `labs/materials/` rather than stay inside this package
