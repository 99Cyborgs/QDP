# Stable

Stable package-owned materials assets belong here when they are not raw seed data.

Current note:

- the canonical stable seed registry currently lives under `data/seed/stable/`
- later phases can promote additional curated, code-adjacent stable assets here if they should not live in `data/`
