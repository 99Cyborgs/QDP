# Packages

These directories are the destination ownership zones for reusable QDP library code.

Current active implementations still live in transitional surfaces such as `tools/workflow/qdp_runtime/`, `modules/`, and sibling donor repos until extraction is complete.

Do not treat an empty package skeleton as migrated authority.

