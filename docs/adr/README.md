# ADR

Destination for consolidation-era architecture decision records.

`docs/decisions/` remains the historical decision surface already present in the repo.
Use `docs/adr/` for new umbrella-monorepo decisions that define package boundaries, authority moves, or migration constraints.

