# E01 MM Mechanism Competition Ledger

| Hypothesis | Mechanism | Observable signature | Confound pathway | Required control | Decision implication |
| --- | --- | --- | --- | --- | --- |
| `H0` | Memoryless field-dependent scalar loss | `1/Qi` follows field value with no true branch memory after allowed drift correction; `H_O` and `A_O` collapse into noise or sham envelope | Slow drift or interpolation error can fake small loop area | Dwell convergence, sham timing, zero-field checkpoints, held-out cooldown test | If H0 explains the data, kill the branch |
| `H1` | Generic metastable hidden state | Dwell-converged hysteresis in `1/Qi` survives sham timing and is not explained by witness or step-lag alone | Residual package drift or cooldown contamination can masquerade as hidden-state memory | Sham timing, package witness, cooldown replication minimum of 3 | Preserve conservative metastable memory branch if H1 beats H0, H3, and H4 |
| `H2` | Vortex candidate with geometry-constrained interpretation | Same-chip matched geometry shows consistent susceptibility ordering together with dwell-converged history dependence | Fabrication mismatch or edge disorder can imitate width dependence | Same-chip matched widths, geometry ordering across all required cooldowns, package witness negative | Allow vortex-candidate promotion path only; do not declare proof |
| `H3` | Quasiparticle-assisted field response | Selected-field `T1` and loss traces show field-step lag or burst recovery that predicts the apparent loop metrics | Transient lag can be mistaken for retained memory when dwell is too short | Dwell-doubling scans, selected-field `T1`, residual inspection around field steps | If H3 explains the primary observable, kill the branch; otherwise keep T1 non-authoritative |
| `H4` | Package or EM drift | Target and witness co-move with elapsed time or field-command timing independent of true sample history | Package modes, cable motion, or common-mode temperature drift can create apparent loop area | Mandatory witness acquisition, sham timing, zero returns, fixed transfer-function check | If H4 explains the primary observable, kill the branch |
