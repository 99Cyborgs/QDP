# M14 Promotion Caps Patch Notes

- Implements the visible-source M14 promotion-cap manager as the single owner of irreversible clipping and fallback completion.
- Uses shared helpers in `qdp_governance.py` so M05 and standalone module runs apply the same clipping semantics.
- This remains a visible-source working patch and does not claim retained-authoritative closure.
