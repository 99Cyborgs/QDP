schema_version: "4.0"
experiment_pack:
  mode: stochastic_ensemble
  base_case: ../configs/validation/seeded_vortex/short_horizon/stochastic_single_positive.yaml
  sweep:
    parameters:
      - name: forcing.a_rf
        values: [0.0, 0.05]
  ensemble:
    member_count: 3
    master_seed: 2026404
  observables:
    - initialization.local_winding.initial_total_signed_winding
    - early_window.mean_abs2_final
  aggregation:
    metrics: [mean, std, min, max]
