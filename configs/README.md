# Configs

Destination for package-scoped source configuration bundles.

Important:

- `config/` remains the current active governance and schema input surface
- `configs/` is the destination layout for consolidation-era package config ownership
- do not silently move active configuration authority from `config/` to `configs/` without updating docs and validation

