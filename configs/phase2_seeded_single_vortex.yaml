base_config: phase1_matrix_base.yaml
metadata:
  case_id: phase2_seeded_single_vortex
  description: "Phase-2 deterministic seeded single-vortex ansatz example"
  tags: ["phase2", "seeded-vortex", "deterministic"]
physics:
  initial_condition: seeded_vortices
  vortex_seeds:
    - x0: 6.0
      y0: 3.0
      winding: 1
forcing:
  a_rf: 0.0
  omega: 0.0
  phase: 0.0
output:
  root_dir: runs/phase2_seeded
  write_fields: false
  write_observables: true
  compression: none
