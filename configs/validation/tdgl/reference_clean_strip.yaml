base_config: ../../tdgl/phase1_matrix_base.yaml
metadata:
  case_id: reference_clean_strip
  description: "Canonical clean deterministic strip for frozen reference checks"
forcing:
  a_rf: 0.0
  omega: 0.0
  phase: 0.0
output:
  root_dir: runs/reference_cases
  write_fields: false
  write_observables: true
  compression: none
