# Integration Tests

Destination test layer for package-to-package and workflow integration coverage.

Current active integration checks include:

- `test_tdgl_material_registry_integration.py`: canonical TDGL configs resolve materials only through `qdp_meta_materials` and carry the registry identity into provenance

Current active repo tests still live in the root `tests/` directory until the suite is reorganized further.
