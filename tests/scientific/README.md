# Scientific Tests

Destination test layer for scientific invariants, regression thresholds, and validation-style evidence checks.

Current active repo tests still live in the root `tests/` directory until the suite is reorganized.
